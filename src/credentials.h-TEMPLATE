// 1. Populate this file with the correct credentials
// 2. Rename file to credentials.h

// WiFi setup
#define WLAN_SSID   "ssid"
#define WLAN_PASS   "password"

#define MQTT_SERVER "mqtt.example.com"
#define MQTT_USERNAME "username"
#define MQTT_PASSWORD "password"

// Server SHA1 fingerprint
// Fetch with:
// > echo | openssl s_client -servername mqtt.example.com -connect mqtt.example.com:8883 2>/dev/null | openssl x509 -noout -fingerprint
#define MQTT_SERVER_FINGERPRINT "00 AA 11 AB etc..."